[build-system]
requires = ["setuptools>=69.5", "wheel"]
build-backend = "setuptools.build_meta"

[tool.ruff]
target-version = "py312"

[tool.pytest.ini_options]
addopts = "--cov=figure_lib"
pythonpath = [
    ".", "src",
]
testpaths = [
    "tests",
]
